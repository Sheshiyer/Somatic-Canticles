# Book 2 Workbench

This folder is now the active downstream Book 2 lane projected from the trilogy-wide intake layer.

- `working/` contains the active first-pass working chapters for `Chapter 09-15`.
- `packets/` contains the governing packet surface for each active chapter pass.

Rules:

1. Treat this folder as an active downstream lane derived from intake, not as the governing scope of `SC_STORYOPS`.
2. `Chapter 09-15` are now active working copies, and `Packet 09-15` are baseline-ready for compiled comparison.
3. Do not edit `working/` without consulting the corresponding packet.
4. Any future rewrite or line pass must update the packet first if the source logic changes.
5. A Book 2 coherence pass and a comparison against the current compiled Book 2 text should happen before any merge-back to canon.
6. Keep `Book 3` material frozen; do not back-propagate late-trilogy revelations into this lane ahead of authority and projection review.
