# Task: Deepen Noesis Image Semantics And Tighten Projection Board

## Plan
- [x] Expand the intake layer with a broader Noesis image-cluster registry drawn from the external research image corpus
- [x] Update the image and concept surfaces so the newly sampled Noesis clusters become usable principles rather than one-off observations
- [x] Replace the current soft `book_projection_board.md` with a more defensible trilogy-placement matrix tied to actual chapter jobs and evidence spines
- [x] Verify the new intake/projection artifacts and record the exact scope of the pass here

## Review
- Added `story/intake/noesis_image_cluster_registry.md` as a dedicated intake artifact so the external Noesis image corpus is now grouped into semantic families before any chapter-level use.
- The first-pass Noesis cluster map now includes seven sampled families:
  - consciousness differentiation
  - compassionate witness
  - pain-signal ethics
  - concealed / revealed truth
  - developmental morphology
  - strategic deception layers
  - tetramorphic integration
- Sampled files grounding those clusters include:
  - `20240304155007.png`
  - `20240304162920.png`
  - `20240612224630.png`
  - `20240612223045.png`
  - `20240827191408.png`
  - `20240828010808.png`
  - `20240921164241.png`
  - `20240921170948.png`
  - `20240921170304.png`
  - `20240922102426.png`
- Deepened the intake surfaces around those clusters:
  - `story/image_index.md` now points to broader Noesis cluster families and ties them to concept links plus placement posture
  - `story/intake/image_principles.md` now includes:
    - `Compassionate witness`
    - `Concealed / revealed truth`
    - `Developmental staging`
    - `Tetramorphic integration`
  - `story/intake/concept_registry.md` now strengthens or adds:
    - `Witness`
    - `Pain as information`
    - `Avatar mutation / developmental continuity`
    - `Concealed truth / structural lie`
- Replaced the old generic projection board with a chapter-job-driven matrix in `story/intake/book_projection_board.md`.
- The new board now makes defensible placement claims instead of loose guesses:
  - `Book 1` owns the diagnosis/patterned-captivity spine
  - `Book 2` owns witness-as-method, developmental continuity, cardiac relation, and three-body governance
  - `Book 3` owns structural lie, false perfection, severance, and authored reality
  - cross-trilogy and bridge families are explicitly labeled rather than left floating
- Verification:
  - confirmed `story/intake/noesis_image_cluster_registry.md` exists in the intake folder
  - confirmed `story/intake/book_projection_board.md` no longer contains patch debris and reads as an evidence matrix
  - confirmed `story/image_index.md` and `story/intake/concept_registry.md` both reflect the new Noesis-derived concepts and image logic
- Canonical safety note:
  - this pass only changed `06_WORKBENCH/SC_STORYOPS/` and `tasks/todo.md`
  - no canonical manuscript files under `02_MANUSCRIPTS/COMPILED/` were edited

# Task: Deepen Trilogy Intake With Wider Vault And Synchronocities Blog

## Plan
- [x] Inspect the wider vault and `01-Projects/synchronocities-blog/` for high-signal material that can strengthen the trilogy-wide intake layer
- [x] Expand `SC_STORYOPS` intake registries so the blog corpus is treated as an explicit synthesis domain rather than an implicit side source
- [x] Extend the image and concept layers with sampled Synchronocities visuals and article-derived principles
- [x] Verify the revised intake tree and record the exact scope of the pass here

## Review
- Added `synchronocities-blog` as an explicit intake surface in the workbench root:
  - `story/README.md` now lists the blog's post corpus and card corpus under extended intake inputs
  - `story/book_rules.md` now defines adjacent synthesis sources as valid but subordinate to doctrine/world-bible/manuscript canon
- Expanded the intake architecture to register the blog directly:
  - added `story/intake/blog_article_registry.md`
  - updated `story/intake/README.md` so the sequence now includes a dedicated blog-article registration step
  - updated `story/intake/vault_source_domains.md` with a `Synchronocities synthesis corpus` domain
  - updated `story/source_lattice.md` with a new `B` legend and an adjacent synthesis/compression domain row
- Deepened the concept layer with article-derived principles from the blog:
  - added or expanded `Consciousness architecture`
  - `Signal-State-Story`
  - `Sacred runtime`
  - `Measurable subtle anatomy`
  - `Endocrine-archetypal channels`
  - `Triangulation protocol`
- Deepened the image layer with sampled Synchronocities visuals:
  - `sync-consciousness-hub.webp`
  - `sync-pain-architecture.webp`
  - `sync-sacred-runtime.webp`
  - `sync-nadi-bioimpedance.webp`
  - `sync-signal-state-story.webp`
  - `sync-nine-muses.webp`
  - `tarot-14-art.webp`
- The blog registry now groups high-signal article clusters instead of flattening the whole repo:
  - consciousness architecture
  - debug and sacred runtime
  - somatic measurement and subtle anatomy
  - triangulation and cross-reference method
  - alchemical compression and tarot handles
- Verification:
  - confirmed `story/intake/blog_article_registry.md` exists alongside the other intake files
  - confirmed `story/image_index.md` now includes a sampled Synchronocities visual table
  - confirmed `story/intake/concept_registry.md` contains the new blog-derived concept rows
  - confirmed the workbench still treats blog material as synthesis/compression support rather than canon
- Canonical safety note:
  - this pass only updated `06_WORKBENCH/SC_STORYOPS/`, `tasks/todo.md`, and `tasks/lessons.md`
  - no edits were made to `02_MANUSCRIPTS/COMPILED/`

# Task: Re-scope SC_STORYOPS To Trilogy-Wide V0.2 Intake

## Plan
- [x] Reframe `06_WORKBENCH/SC_STORYOPS/` from a Book 2-first proving ground into a trilogy-wide `v0.2` intake layer using the InkOS-style `story/` architecture as a repo-native framework
- [x] Add trilogy-first workbench artifacts for vault resource extraction, image principle extraction, lore/worldbuilding consolidation, and concept mapping before assigning work to individual books
- [x] Rewrite the workbench operating contract so book folders are downstream staging lanes rather than the primary starting point for this cycle
- [x] Verify the revised workbench structure and record the corrected operating model in this file

## Review
- Recorded the user correction in `tasks/lessons.md` so future `SC_STORYOPS` passes do not collapse a trilogy-wide intake request into a single-book pilot.
- Reframed the workbench root around trilogy-wide `v0.2` intake:
  - `story/README.md` now defines the active sequence as `clarify -> intake -> plan -> tasks -> book assignment`
  - `story/book_rules.md` now treats trilogy-first extraction as the governing rule
  - `story/clarify_log.md` now locks the sequencing policy and adds a projection-freeze blocker
  - `story/source_lattice.md` now maps source domains and authority layers before chapter inheritance
  - `story/image_index.md` now works at the principle-family level rather than treating Book 2 chapter mapping as the main job
- Added a dedicated trilogy-intake layer under `story/intake/`:
  - `README.md`
  - `vault_source_domains.md`
  - `concept_registry.md`
  - `lore_worldbuilding_map.md`
  - `image_principles.md`
  - `book_projection_board.md`
- The new intake layer now captures the corrected operating model:
  - vault resources are grouped by usable story domains
  - worldbuilding and lore are separated by authority level
  - image intelligence is converted into reusable narrative principles
  - concepts are registered before they are assigned to a book
  - book fit remains explicitly `soft` until source, image, and lore support converge
- Reworked chapter-lane semantics:
  - added `story/chapters/README.md`
  - scaffolded `story/chapters/book_1_anamnesis_engine/README.md`
  - scaffolded `story/chapters/book_3_the_ripening/README.md`
  - rewrote `story/chapters/book_2_myocardial_chorus/README.md` so the existing Book 2 packet set is preserved as provisional downstream material rather than the default scope of the workbench
- Verification:
  - confirmed the revised workbench now contains the trilogy-intake files plus per-book lane scaffolds
  - confirmed the existing Book 2 packets and working chapter copies remain present
  - confirmed the active changed surfaces for this correction are `06_WORKBENCH/SC_STORYOPS/`, `tasks/todo.md`, and `tasks/lessons.md`
- Canonical safety note:
  - this correction pass did not edit the canonical `02_MANUSCRIPTS/COMPILED/` files
  - the repo still contains unrelated pre-existing dirty files outside this work scope

# Task: Initialize SC_STORYOPS Research + Dialogue Workbench

## Plan
- [x] Create `06_WORKBENCH/SC_STORYOPS/` as an isolated repo-local staging lane with an InkOS-shaped `story/` workspace
- [x] Populate the core coordination files from canonical manuscript, editorial doctrine, world bible, Brandmint, and Noesis research inputs without editing `02_MANUSCRIPTS/COMPILED/`
- [x] Copy Book 2 source chapters from `02_MANUSCRIPTS/CHAPTERS/` into the workbench and create compact chapter packets for surgical dialogue and mapping passes
- [x] Verify the new workbench structure exists, the coordination files are populated, and this setup pass left canonical manuscript files untouched

## Review
- Created the isolated staging lane at `06_WORKBENCH/SC_STORYOPS/story/` with the full coordination surface required for the new `clarify -> plan -> tasks` cycle:
  - `README.md`
  - `outline.md`
  - `book_rules.md`
  - `chapter_summaries.md`
  - `subplot_board.md`
  - `emotional_arcs.md`
  - `character_matrix.md`
  - `style_guide.md`
  - `dialogue_voice_matrix.md`
  - `clarify_log.md`
  - `source_lattice.md`
  - `image_index.md`
- Populated the workbench from the current canon and authority stack rather than inventing a new continuity layer:
  - trilogy doctrine and vocabulary materials under `03_EDITORIAL/`
  - world-bible structure, character, protocol, and tarot-biological mapping files
  - `brandmint-input/somatic-canticles/generation-manifest.json`
  - external Noesis research excerpts and sampled image semantics
- Established the canonical/staging contract inside the workbench:
  - `COMPILED/` remains canonical and read-only for setup
  - working derivatives are sourced from `02_MANUSCRIPTS/CHAPTERS/`
  - future prose edits must route through chapter packets with source and dialogue support
  - the `22`-arcana vs `27`-chapter mismatch is explicitly treated as a clarification problem, not silently baked into new prose
- Initialized Book 2 as the first execution lane:
  - copied the seven canonical Book 2 source chapters into `story/chapters/book_2_myocardial_chorus/working/`
  - created seven compact chapter packets under `story/chapters/book_2_myocardial_chorus/packets/`
  - each packet includes canonical excerpt, mapped sources, mapped images/motifs, dialogue targets, and a surgical-pass checklist
- Clarification and mapping outputs now cover the required high-impact issues:
  - `clarify_log.md` locks the current `27`-chapter canon, downgrades the `22` arcana set to a supporting taxonomy rather than chapter parity, and records the dual-readiness policy
  - `source_lattice.md` fully maps Book 2 chapters first and scaffolds the rest of the trilogy
  - `image_index.md` links Brandmint asset families and sampled external Noesis images to Book 2 narrative use
  - `dialogue_voice_matrix.md` separates narration rules from dialogue rules and differentiates Jian, Sona, Gideon, and Corv across sentence length, vocabulary register, conflict behavior, and avoidance patterns
- Verification performed:
  - confirmed the workbench file tree exists under `06_WORKBENCH/SC_STORYOPS/`
  - confirmed Book 2 working copies and packets exist for Chapters `09` through `15`
  - confirmed the new workbench README and clarify log encode the staging contract and contradiction policy
- Canonical safety note:
  - this pass created the new workbench lane and updated `tasks/todo.md`
  - `git status --short` shows pre-existing dirty files under `02_MANUSCRIPTS/COMPILED/`, but no setup edits in this pass were made to those canonical manuscript files

# Task: Audit Latest Manuscript Improvements + Readiness Gap

## Plan
- [x] Audit the newest `02_MANUSCRIPTS/COMPILED` and `02_MANUSCRIPTS/EXPORTS` artifacts to identify what materially improved in the latest pass
- [x] Inspect the omnibus and book files for structural, editorial, glossary, citation, and image-linkage signals that indicate true content maturity versus packaging polish
- [x] Compare the current manuscript against `03-Resources/` and `/Users/sheshnarayaniyer/Documents/noesis/Research/` to assess how much of the available research and image intelligence is actually mapped into the books
- [x] Record a review in this file summarizing readiness, strengths, blockers, and the highest-leverage next steps toward moving beyond the current ~30% mapped state

## Review
- Latest improvements are real, but they are mostly assembly and export improvements rather than deep manuscript-comprehension improvements.
- Confirmed packaging gains:
  - `02_MANUSCRIPTS/COMPILED/Somatic_Canticles_Trilogy_Omnibus_CLEAN.md` was rebuilt on `2026-04-21`.
  - `02_MANUSCRIPTS/EXPORTS/` now contains regenerated `html`, `epub`, and `pdf` outputs plus `export-manifest.json` with hashes and generation metadata.
  - The normalization/handoff docs under `00_PLANNING_AND_ROADMAP/TRILOGY_NORMALIZATION_PROGRAM/` correctly establish `COMPILED/` as the canonical manuscript surface.
- Current readiness splits into two different numbers:
  - packaging/export readiness: high enough for downstream consumption
  - research/image tapestry readiness: still roughly `30%`, and that estimate is supported by the evidence below
- Why the `~30%` mapping estimate is credible:
  - the omnibus is exportable but not source-traceable: no bibliography, source-notes, citation layer, or research-map file was found in the live manuscript/export surfaces
  - the manuscript and HTML export contain no embedded image/figure structure, so the existing visual corpus is not yet functioning as part of the reading surface
  - the glossary only contains `18` entries, which is too thin for the conceptual density and external research depth surrounding the project
  - `03-Resources/` contains a very large available lattice (`12847` files, `2989` markdown notes, `2356` PDFs, `1696` images), but the manuscript does not yet expose a durable mapping layer that would let an editor prove how that depth is distributed across chapters
  - `/Users/sheshnarayaniyer/Documents/noesis/Research/` is highly image-heavy (`106` images out of `110` files), but `Images.md` is mostly a raw embed dump without semantic indexing, captions, or topic mapping; the image intelligence exists, but retrieval/mapping is still weak
- Structural manuscript findings:
  - total omnibus size is only about `41.4k` words, which is materially smaller than the surrounding trilogy/worldbuilding promise surfaces imply
  - Book 2 is especially compressed at about `4.7k` words across seven chapters, which makes it read more like a polished conceptual bridge than a fully metabolized novel volume
  - chapter distribution is highly uneven: Book 2 chapters range from roughly `565` to `724` words, while Book 3 carries most of the actual narrative mass
- Ecosystem sync findings:
  - the live manuscript has `27` chapters
  - the brand/wiki collateral still repeatedly claims `22` chapters mapped to Major Arcana
  - the visual system currently contains `22` arcana cards, `8` anatomy plates, `3` covers, `8` logos, and `1` brand-kit board
  - result: the manuscript canon, the tarot/biology promise, and the visual asset architecture are not yet reconciled into one source-mapped system
- Supporting assets do exist outside the manuscript:
  - `03_EDITORIAL/SCIENTIFIC_CLAIMS_MASTER_LIST.md` shows some real biology validation work
  - `brand-wiki-site/src/content/docs/` contains richer explanatory and positioning language than the book exports themselves
  - this means the project is not missing intelligence; it is missing integration into the manuscript surface
- Highest-leverage next steps:
  - create an internal chapter-by-chapter source map that links each chapter to concrete vault notes, research excerpts, and image families
  - create an image intelligence index for `/Users/sheshnarayaniyer/Documents/noesis/Research/` so the current timestamp-named images become retrievable by topic, motif, chapter, and biological/philosophical concept
  - reconcile the `27`-chapter manuscript with the `22`-arcana downstream promise instead of leaving both active at once
  - expand Book 2 from compressed bridge mode into a full dramatic and research-bearing volume before treating the trilogy as truly release-ready
  - add an internal research/backmatter layer for editorial use, even if it is not exposed in the reader-facing edition

# Task: Rewire Astro Wiki To Brandmint Asset Handoff

## Plan
- [x] Capture the user correction in `tasks/lessons.md` so the wiki stays aligned to the Brandmint handoff contract
- [x] Replace the visual-atlas folder-glob inference with a Brandmint-driven asset map built from `brandmint-input/somatic-canticles/generation-manifest.json` and `brand-wiki-site/src/images/media-manifest.json`
- [x] Update the wiki data layer, sidebar, and home-page asset flow copy so counts, ordering, labels, and family sequencing come from the Brandmint pipeline outputs
- [x] Fix atlas card presentation so portrait assets stop getting cropped like generic gallery thumbnails
- [x] Run `npm run build` in `brand-wiki-site/` and record the verification result

## Review
- Rewired `brand-wiki-site/src/data/wiki.ts` to build the shared asset catalog from:
  - `brandmint-input/somatic-canticles/generation-manifest.json`
  - `brand-wiki-site/src/images/media-manifest.json`
- Added manifest consistency checks so the build fails if the Brandmint run inventory and synced Astro image inventory drift apart by family.
- Replaced the atlas page’s raw `src/images/**/*` folder inference with typed Brandmint-backed asset records that preserve:
  - family ordering
  - Brandmint asset IDs
  - Brandmint display names
  - current-run counts and cost metadata
- Updated the atlas UI so image frames use contained media instead of forced 4:3 crop boxes, preventing tall or centered compositions from being clipped like generic thumbnails.
- Updated the sidebar and home page copy so the asset flow is explicitly described as a Brandmint handoff, not a manually curated gallery.
- Verification:
  - `npm run build` in `brand-wiki-site/` passed
  - generated HTML includes:
    - `42 manifest-backed visuals`
    - `Follow the Brandmint asset flow`
    - asset IDs such as `ARCANA-07-CHARIOT`

# Task: Apply Packaged Brandmint Patch To Real Repo

## Plan
- [ ] Inspect the real Brandmint repo status and overlapping local edits before applying the packaged patch
- [ ] Apply `/tmp/brandmint-somatic-registry-wiki-handoff-aa70c88.patch` into the real Brandmint repo without overwriting unrelated local work
- [ ] Run focused Brandmint verification in the real repo and document the exact result

# Task: Astro Wiki Asset Flow + Brandmint Patch Packaging

## Plan
- [x] Preserve the verified Brandmint changes inside the clean worktree so they can be committed or transplanted without touching the dirty parent checkout
- [x] Promote the new visual asset families into the Astro wiki data model so covers, logos, anatomy, arcana, and the brand board are first-class navigation objects
- [x] Update the wiki menus, home page, atlas anchors, and dossier flow to route readers through the asset map instead of treating visuals as a side path
- [x] Verify the Astro site build and confirm the clean-worktree Brandmint patch is ready for handoff

## Review
- Astro wiki implementation:
  - promoted asset families into `brand-wiki-site/src/data/wiki.ts` as first-class metadata with counts, atlas anchors, and dossier entry points
  - updated `brand-wiki-site/src/components/Navigation.astro` so the sidebar now exposes a dedicated visual-family menu, not just dossier groups
  - updated `brand-wiki-site/src/pages/index.astro` to add an asset-flow section that routes readers from the brief into covers, logos, anatomy, arcana, and the brand board
  - updated `brand-wiki-site/src/pages/visual-atlas.astro` so every family has a stable anchor plus linked entry dossiers
  - updated `brand-wiki-site/src/layouts/DocLayout.astro` so dossier pages now show related visual-family links in the rail
- Astro verification:
  - `npm run build` in `brand-wiki-site/` passed
  - built output confirms the new nav and flow copy:
    - sidebar includes `Visual families`
    - dossier pages include `Asset flow`
    - atlas sections include `Entry dossiers`
- Brandmint patch packaging:
  - the original `/tmp/brandmint-origin-main-20260419` worktree was gone, so a fresh clean clone was created at `/tmp/brandmint-packaging-20260420`
  - the clone’s `origin/main` had moved to `424df6c`, so reconstruction was branched from the intended pipeline baseline `e04b892` on `codex/somatic-registry-wiki-handoff`
  - upstream Brandmint patch now restored in:
    - `scripts/run_pipeline.py`
    - `scripts/generate_pipeline.py`
    - `scripts/asset_registry.py`
    - `brandmint/core/asset_registry.py`
    - `brandmint/core/wave_planner.py`
    - `tests/test_reference_selection_and_asset_exclusions.py`
    - `tests/test_pipeline_catalog_and_costs.py`
- Brandmint verification:
  - `python3 -m py_compile scripts/run_pipeline.py scripts/generate_pipeline.py scripts/asset_registry.py brandmint/core/asset_registry.py brandmint/core/wave_planner.py`
  - `python3 -m pytest tests/test_reference_selection_and_asset_exclusions.py tests/test_generate_pipeline_spec_lock.py tests/test_visual_backend.py tests/test_visual_backend_fallback_chain.py tests/test_generate_pipeline_template.py tests/test_pipeline_catalog_and_costs.py -q`
    - result: `36 passed, 4 skipped`
  - `python3 scripts/run_pipeline.py preview --config /Volumes/madara/2026/twc-vault/01-Projects/Somatic-Canticles/brandmint-input/brand-config.yaml --json`
    - selected assets: 42
    - composition: `2A` + 41 Somatic custom assets
    - estimated cost: `$6.72`
  - `python3 scripts/generate_pipeline.py /Volumes/madara/2026/twc-vault/01-Projects/Somatic-Canticles/brandmint-input/brand-config.yaml`
    - regenerated manifest with 42 assets and `$6.72` estimate
    - custom identity assets now emit Nano Banana generation with archive-preserving slugs
    - custom arcana/anatomy/covers now emit from `generate-illustrations.py` with resolved per-asset prompt metadata

# Task: Somatic-Canticles Brandmint Registry + Preview Alignment

## Plan
- [x] Create a clean Brandmint worktree from `origin/main` instead of editing the dirty local checkout
- [x] Inspect the clean worktree versions of `run_pipeline.py`, `generate_pipeline.py`, registry loading, and wave docs
- [x] Build a Somatic-Canticles-specific asset registry that models `arcana`, `anatomy`, `covers`, and `logos`
- [x] Wire `generation.asset_registry_paths` in the Somatic-Canticles config so those families are selectable
- [x] Patch `preview --json` to use the same exclusion-aware registry selection and budget path as `generate`
- [x] Update the Brandmint textual docs and skill files so the current wave pipeline includes NotebookLM and wiki handoff stages
- [x] Run focused verification in the clean worktree and record evidence here before marking the task done

## Review
- Safe upstream workspace:
  - created clean Brandmint worktree at `/tmp/brandmint-origin-main-20260419`
  - fetched `origin/main` to `e04b892` and left the dirty parent checkout untouched
- Upstream Brandmint code changes in the clean worktree:
  - `scripts/run_pipeline.py`
    - `preview --json` now resolves the same exclusion-aware asset set as generate instead of hardcoding the legacy catalog
    - budget calculation now reads registry metadata for custom assets and keeps config-driven poster/sequence lanes explicit
  - `scripts/asset_registry.py`
  - `brandmint/core/asset_registry.py`
  - `brandmint/core/wave_planner.py`
    - configured `asset_registry_paths` now layer on top of the core registry instead of replacing it
    - wave planning now places non-static domain assets into waves by generator so custom registry entries appear in the plan
  - `scripts/generate_pipeline.py`
    - custom asset prompts now render with per-asset registry metadata
    - custom `identity` assets can use `nano-banana-pro`
    - `illustrations` now supports domain-specific assets
    - custom slugs remain archive-aligned for filenames while generated Python identifiers stay valid
- Somatic-Canticles registry/config wiring:
  - added `brandmint-input/somatic-canticles-asset-registry.yaml`
  - registry models:
    - 22 `arcana` assets
    - 8 `anatomy` assets
    - 3 `covers`
    - 8 `logos`
  - updated `brandmint-input/brand-config.yaml` with:
    - `generation.asset_registry_paths`
    - expanded `generation.excluded_assets` to suppress stock non-Somatic families
    - `prompts.extended` templates for arcana, anatomy, covers, and logos
    - `somatic-canticles` domain tag
- Brandmint docs/UI alignment:
  - updated `SKILL.md`
  - updated `CLAUDE.md`
  - updated `skills/publishing/wiki-doc-generator/SKILL.md`
  - updated `skills/publishing/markdown-to-astro-wiki/SKILL.md`
  - updated `ui/src/App.tsx` publishing-wave copy so Wave 8 is described as pipeline handoff, not a post-pipeline extra
- Real-command verification from the clean worktree:
  - `python3 scripts/run_pipeline.py preview --config /Volumes/madara/2026/twc-vault/01-Projects/Somatic-Canticles/brandmint-input/brand-config.yaml --json`
    - selected assets: 42
    - composition: `2A` + 41 Somatic custom assets
    - families present: covers, logos, arcana, anatomy
    - estimated cost: `$6.72`
    - every selected custom asset is on `nano-banana-pro`
  - `python3 scripts/generate_pipeline.py /Volumes/madara/2026/twc-vault/01-Projects/Somatic-Canticles/brandmint-input/brand-config.yaml`
    - regenerated `brandmint-input/somatic-canticles/generation-manifest.json` with 42 assets
    - regenerated scripts only for the required families:
      - `generate-anchor.py`
      - `generate-identity.py`
      - `generate-illustrations.py`
      - `generate-posters.py`
  - generated-script sanity checks:
    - `brandmint-input/somatic-canticles/scripts/generate-identity.py` now contains `PROMPT_LOGO_GLASS_CLASSIC` and `gen_nano_banana("LOGO-GLASS-CLASSIC", ...)`
    - `brandmint-input/somatic-canticles/scripts/generate-illustrations.py` now contains `PROMPT_07_CHARIOT_CARDIAC_COHERENCE`, `urls_07_chariot_cardiac_coherence`, and `gen_nano_banana("ARCANA-07-CHARIOT", ...)`
- Focused upstream regression suite:
  - `python3 -m pytest tests/test_reference_selection_and_asset_exclusions.py tests/test_generate_pipeline_spec_lock.py tests/test_visual_backend.py tests/test_visual_backend_fallback_chain.py tests/test_generate_pipeline_template.py tests/test_pipeline_catalog_and_costs.py -q`
  - result: `37 passed, 4 skipped`

# Task: Mobile Wiki Fix + Brandmint Pipeline Parity Check

## Plan
- [x] Audit the current wiki UI and Brandmint source-of-truth drift
- [x] Patch the Astro wiki for mobile-safe media/table behavior and restore the documented brand palette
- [x] Restore repo-local Brandmint intake artifacts (`product.md` + `brand-config.yaml`)
- [x] Run the upstream Brandmint preview/generate paths from `/Volumes/madara/2026/twc-vault/01-Projects/brandmint`
- [x] Compare Brandmint-selected/generated assets against the committed wiki archive taxonomy
- [x] Document verification evidence, pipeline drift, and required next-step constraints

## Review
- Front-end fixes completed in this pass:
  - home page now carries brand-true narrative pillars instead of a generic archive lead
  - home hero media, reading path cards, featured dossiers, and doc gallery preserve width-oriented visuals on narrow screens via horizontal snap rows
  - prose tables now have horizontal containment and minimum cell widths so wide content no longer breaks mobile layout
  - the site color system now maps back to the documented Somatic Canticles palette instead of the prior parchment-heavy drift
- Local site verification:
  - `npm run build` in `brand-wiki-site/` passed after the layout + palette changes
- Reproducible Brandmint intake restored in Git:
  - `brandmint-input/product.md`
  - `brandmint-input/brand-config.yaml`
- Upstream Brandmint verification completed from `/Volumes/madara/2026/twc-vault/01-Projects/brandmint`:
  - `bm plan recommend --config /Volumes/madara/2026/twc-vault/01-Projects/Somatic-Canticles/brandmint-input/brand-config.yaml` succeeded
  - `python3 scripts/run_pipeline.py preview --config /Volumes/madara/2026/twc-vault/01-Projects/Somatic-Canticles/brandmint-input/brand-config.yaml --json` succeeded
  - direct `asset_registry.select_assets(...)` evaluation on the same config succeeded
  - `bm visual generate --config /Volumes/madara/2026/twc-vault/01-Projects/Somatic-Canticles/brandmint-input/brand-config.yaml` succeeded
- Preview vs generate drift is real:
  - `preview --json` still recommends excluded assets `2B`, `2C`, and `5A`, which means the preview/recommendation path does not honor `generation.excluded_assets`
  - the actual selector path (`asset_registry.select_assets`) does honor exclusions and returns 11 selected assets:
    - `2A`
    - `3A`
    - `3B`
    - `4A`
    - `5B`
    - `4B`
    - `7A`
    - `8A`
    - `PITCH-HERO`
    - `3C`
    - `EMAIL-HERO`
  - the generated manifest also honors exclusions and stays on `nano-banana-pro` for all selected assets
- Cost-control finding:
  - the actual generate path avoids Flux/Recraft credit spend for this config
  - the preview path is still unsafe as the sole budgeting signal because it overstates excluded Flux/Recraft lanes
- Current parity with the committed wiki archive is not achieved:
  - wiki archive inventory: 42 committed files
  - category counts:
    - `arcana`: 22
    - `anatomy`: 8
    - `covers`: 3
    - `logos`: 8
    - `brand-kit`: 1
  - generated Brandmint manifest: 11 assets
    - `2A`
    - `3A`
    - `3B`
    - `3C`
    - `4A`
    - `4B`
    - `5B`
    - `7A`
    - `EMAIL-HERO`
    - `8A`
    - `PITCH-HERO`
  - only the brand-kit board has a clear one-to-one overlap with the current archive
  - the stock Brandmint registry does not contain Somatic-Canticles-specific asset families for `arcana`, `anatomy`, `covers`, or the existing `logos` library, so the current wiki archive cannot be regenerated one-to-one from the default registry
- Additional repo drift still present:
  - the committed image manifest still points at missing external source paths:
    - `/Volumes/madara/2026/Somatic-Canticles/somatic-canticles/generated`
    - `/Volumes/madara/2026/Somatic-Canticles/wiki-output`
  - repo-local `somatic-canticles/` and `wiki-output/` folders remain placeholders, not the real asset-generation source of truth
- Required next-step constraint if exact parity matters:
  - add `generation.asset_registry_paths` with a Somatic-Canticles-specific registry/template family that models the current `arcana`/`anatomy`/`covers`/`logos` archive, or intentionally migrate the wiki to the stock Brandmint asset taxonomy instead

# Task: Brandmint Wave 0-9 Audit + GitHub Repo Packaging

## Plan
- [x] Reconcile the requested Brandmint wave model against the upstream Brandmint source of truth and document any missing wave definitions explicitly
- [x] Produce a wave-by-wave audit for Somatic Canticles covering:
  - product definition
  - assets already present in this repo
  - assets represented in the Brandmint manifest
  - assets still missing or not yet generated locally
- [x] Create a reusable standalone export script that assembles a GitHub-ready repo package from the current Somatic Canticles source-of-truth files
- [x] Generate the standalone repo package locally and verify its structure, key files, and asset counts
- [x] Prepare the GitHub publication step with a safe default repo slug/visibility assumption and record any external-action blockers in the review

## Review
- Brandmint wave reconciliation:
  - upstream Brandmint currently defines Wave `0` through Wave `8`; there is no native Wave `9`
  - for this project, Wave `9` is explicitly treated as the GitHub publication layer after Brandmint's Wave `8` docs/wiki handoff
- Wave audit created at:
  - `00_PLANNING_AND_ROADMAP/BRANDMINT_WAVE_0_9_AUDIT.md`
- Audit conclusions:
  - product is already well-defined as `Somatic Canticles`, a premium hard-SF / consciousness-literature trilogy
  - locally present core brand assets are complete for the current visual system:
    - `22` arcana
    - `8` anatomy
    - `3` covers
    - `8` logos
    - `1` brand-kit board
  - missing downstream package layers are primarily:
    - Wave 6 distribution outputs
    - Wave 7 NotebookLM/publishing artifacts
    - standalone GitHub repo publication
- Export tooling created:
  - `scripts/export_brandmint_repo.py`
- Export verification:
  - `python3 -m py_compile scripts/export_brandmint_repo.py`
  - `python3 scripts/export_brandmint_repo.py --force`
  - exported package path: `/tmp/somatic-canticles-brandmint-kit`
  - export manifest confirms:
    - `42` total assets
    - `$6.72` estimated cost
    - `84` total API calls
    - manifest counts and copied site counts match
- Export cleanup hardening:
  - first pass copied transient directories like `node_modules`, `.astro`, and `.vercel`
  - exporter was patched to exclude transient build/runtime directories and re-verified
- Local git packaging:
  - initialized standalone repo in `/tmp/somatic-canticles-brandmint-kit`
  - staged export successfully
  - created local initial commit:
    - `46e8c08` `feat: initial Somatic Canticles Brandmint kit`
- GitHub publication blocker:
  - `gh auth status` reports the `Sheshiyer` token is invalid
  - result: remote repo creation / push could not be completed from this session
  - prepared safe default publish target in `/tmp/somatic-canticles-brandmint-kit/PUBLISHING.md`:
    - repo slug: `somatic-canticles-brandmint-kit`
    - visibility: `private`
