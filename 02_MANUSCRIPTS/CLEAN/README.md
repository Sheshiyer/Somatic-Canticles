# Deprecated Path

This directory is intentionally deprecated.

For all current manuscript work, use:

- `../CHAPTERS/` for chapter-by-chapter editorial files
- `../COMPILED/` for the canonical clean book files and omnibus

`CLEAN/` is retained only so older references do not silently point at nothing.
